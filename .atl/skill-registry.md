# Skill Registry — kie-cli

Generated by `sdd-init`. Rescan with the `skill-registry` skill after installing/removing skills.

## Project Skills

| Name | Path | Trigger (from frontmatter) |
|------|------|------------------------------|
| kie-media | code/kie/skills/kie-media/SKILL.md (also synced to code/skills/kie-media/SKILL.md via `scripts/sync-skill.mjs`) | Generate images/video with the `kie` CLI (KIE.ai: Nano Banana 2, Seedream, Grok Imagine 2, Kling 3.0, Kling O3, Seedance 2.5, MiniMax H3, Wan 3.0, Gemini Omni 1.1, Veo 3) under strict spend limits. Trigger: user asks to generate/create/render/edit an image or video, or mentions KIE, Kling, Veo, Seedance, Wan, Grok Imagine, Gemini Omni, Nano Banana, "genera una imagen", "hazme un video". |

No project-level convention index files found (no root `CLAUDE.md`, `AGENTS.md`, `.cursorrules`, or `GEMINI.md` in this repo). User-level `~/.claude/CLAUDE.md` carries the Engram + Agent Teams Lite protocol (not project-specific).

## Compact Rules (auto-resolved for sub-agent injection)

Inject these into any sub-agent prompt touching `code/kie/**` (source, tests) or `code/kie/skills/**` / `code/skills/**` (the kie-media skill):

- **Zero runtime dependencies**: `kie/package.json` `dependencies` MUST stay `{}`. Never add a runtime dependency. Dev-only tooling (`typescript`, `@types/node`) is fine.
- **Network boundary**: ALL HTTP calls to KIE.ai MUST go through `kie/src/client.ts` (`KieClient`). Do not call `fetch` directly elsewhere.
- **Spend safety boundary**: ALL spend checks MUST go through `kie/src/guard.ts` (`checkSpend`) BEFORE a task is created, and every created/completed/failed task MUST be recorded via `kie/src/ledger.ts` (`appendLedger`/`readLedger`).
- **No callbacks**: the CLI MUST NEVER send `callBackUrl` to KIE.ai. This is enforced in `kie/src/client.ts`, `kie/src/commands/generate.ts`, and `kie/src/catalog.ts` (throws if `callBackUrl` is present in the input).
- **ESM + Node >=20**: `kie/tsconfig.json` targets ES2022/NodeNext; `kie/package.json` has `"type": "module"`. Use `.js` extensions in relative imports (NodeNext resolution).
- **Build/typecheck/test commands**: `npm run build` (tsc + `scripts/sync-skill.mjs`), `npm run typecheck` (`tsc --noEmit`), `npm test` (`npm run build && node --test dist/test/*.test.js`) — all run from `code/kie/`.
- **Skill sync**: the canonical source is `code/skills/kie-media/**` (repo root). `code/kie/scripts/sync-skill.mjs` (run by `npm run build`) copies it INTO `code/kie/skills/kie-media/**` (the npm-bundled copy) and overwrites it. Edit ONLY the root copy; edits to `code/kie/skills/` are silently lost on build.
- **Commit trailers**: the user's own preference (captured separately, see `sdd-init/kie-cli` in engram) is that KIE-cli commits carry NO Claude trailers (no `Co-Authored-By`, no `Claude-Session` line) — this overrides any global default trailer instructions when committing in this repo.
- **Tests are network-free**: tests under `code/kie/test/*.test.ts` inject a fake `fetch` implementation (see `client.test.ts`'s `fakeFetch` helper) — never make real HTTP calls in tests.

## User-Level Skills (global, not project-specific)

Scanned from `~/.claude/skills/`. Only the SDD family and general-purpose ones are relevant to this project's workflow; the rest (cloudflare-*, agents-sdk, shadcn, etc.) are unrelated to kie-cli's stack and are omitted from compact-rule injection here.

Relevant to kie-cli work: `sdd-init`, `sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`, `sdd-onboard`, `skill-creator`, `skill-registry`, `code-review`, `simplify`, `security-review`.
