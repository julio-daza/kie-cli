# dependencies / build
node_modules/
dist/
# bundled copy of skills/kie-media created by `npm run build` in kie/
kie/skills/
.astro/
*.tgz

# local state — never commit keys, ledgers or generated media
.env
.env.*
!.env.example
kie-media/
*.key
ledger.jsonl

# editors / OS
.DS_Store
.vscode/
.idea/

# the website lives in julio-daza/kie-landing; a leftover local copy is ignored
/landing/
